<%= simple_form_for(@collection) do |f| %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.hidden_field :id, value: @restaurant_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class:'btn btn-primary'  %>
  </div>
<% end %>
